const initState = {
    connected: false
};

export default function reducer(state = initState, action) {
    switch (action.type) {
      /*case TOGGLE_THEME_MODE:*/
      default:
        return state;
    }
  }