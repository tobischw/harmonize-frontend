const initState = {
    connected: false,
    clientName: '',
    clientId: '',
    sources: [],
    realPosition: 0
};

export default function reducer(state = initState, action) {
    switch (action.type) {
      default:
        return state;
    }
}